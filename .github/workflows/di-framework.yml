name: Release di-framework maven package

on:
  workflow_dispatch:
    inputs:
      env:
        default: 'main'
        required: true
        type: string
        options:
          - 'main'
          - 'rc'
          - 'dev'
        description: The environment type pipeline is run in
  push:
    branches:
      - master
    paths:
      - 'di-framework/**'
      - '.github/workflows/di-framework.yml'
      - '.github/workflows/templates/java_release.yml'

jobs:
  determine_env:
    runs-on: ubuntu-latest

    steps:
      - name: Determine environment
        id: environment
        run: |
          if [ "${{ github.event_name }}" -eq "workflow_dispatch" ]; then
            echo "env="${{ github.event.inputs.env }}" >> $GITHUB_ENV
          else
            echo "env=dev" >> $GITHUB_ENV

    outputs:
      env: ${{ env.env }}

  release:
    needs: determine_env
    uses: ./.github/workflows/templates/java_release.yml
    with:
      env: ${{ needs.determine_env.outputs.env }}
      project: 'di-framework'